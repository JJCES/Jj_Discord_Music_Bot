import { Command } from '../../interface/Types';

const command: Command = {
    description: "현재 재생중인 곡의 리스트를 보여줍니다.",
    dm: false,
    management: false,
    run: (client, message, args) => {

    }
};

export default command;